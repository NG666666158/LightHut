---
description: 修改代码后必须在本机重启 Spring Boot；助手须实际执行重启而非只口头提醒
alwaysApply: true
---

# 修改后必须重启并验收

对本仓库做任何会影响运行结果的修改后（Java、`src/main/resources` 下的模板与静态资源、`pom.xml`、配置等），**在结束该轮任务前**须完成下列之一：

- **优先**：由助手在终端**实际执行**构建与启动（用户要求用真实环境验收，不要只改文件）。
- 若端口被占用：先结束占用 **8080** 的进程（Windows：`netstat -ano | findstr :8080`，再 `taskkill /PID <pid> /F`）。

标准流程（项目根目录）：

1. `mvn -DskipTests package`
2. `java -jar target\hollow-1.0.0.jar`（长时间运行可用后台方式，并查看 `terminals` 输出确认已监听 8080）

**禁止**：只写「请自行重启」而不执行命令（除非用户明确说不要动终端）。

**易漏项**：只改了 `cheer.html`、`home.html` 等 Thymeleaf 模板时，也必须重启（或确保 devtools 已热重载）；否则浏览器里仍是旧页面。

# 复制为 .env 后由 docker compose 读取（勿提交真实 .env）
#
# MySQL（与 docker-compose.yml 中 hollow 用户一致）
MYSQL_ROOT_PASSWORD=change_me_root
MYSQL_PASSWORD=change_me_hollow

# 宿主机映射端口：直接访问 Spring（不设 nginx profile 时）
APP_PORT=8080

# 使用 profile nginx 时，宿主机 HTTP 端口
HTTP_PORT=80
